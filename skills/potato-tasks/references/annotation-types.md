<!-- Generated by scripts/generate_references.py. Do not edit by hand. -->

# Annotation types

Potato has 61 annotation types. Set one as `annotation_type` on each entry in `annotation_schemes`.

Every type also accepts these, regardless of which one you pick:

- Required: `annotation_type`, `description`, `name`
- Optional: `display_logic`, `humanize_labels`, `label_requirement`, `layout`, `required`, `session_level`, `turn_binding`, `turn_label`, `turn_level`

The examples below are lifted from configs in `examples/` that use each type, so they are known to run.

## Index

- [`agent_interaction_graph`](#agent-interaction-graph) — Clickable agent-interaction graph: mark critical-path nodes + flag problematic edges
- [`agent_scorecard`](#agent-scorecard) — Per-agent + per-team scorecard with optional milestones (MultiAgentBench-style)
- [`audio_annotation`](#audio-annotation) — Audio segmentation and annotation with waveform visualization
- [`bws`](#bws) — Best-Worst Scaling: select the best and worst item from a set
- [`card_sort`](#card-sort) — Drag-and-drop card sorting into predefined or user-created groups
- [`code_review`](#code-review) — GitHub PR-style code review with inline comments and file ratings
- [`confidence`](#confidence) — Confidence rating meta-annotation for any primary annotation
- [`conjoint`](#conjoint) — Discrete choice conjoint analysis with side-by-side profile comparison
- [`consensus_tracking`](#consensus-tracking) — Tag discussion acts per turn (proposal/agreement/disagreement/decision/concession) with cross-turn links to referenced proposals
- [`constant_sum`](#constant-sum) — Allocate a fixed budget of points across categories
- [`context_attribution`](#context-attribution) — Tag how each turn uses earlier context (used correctly / hallucinated / ignored) with links to the source turn
- [`coreference`](#coreference) — Coreference chain annotation for grouping mentions of the same entity
- [`emergent_behavior`](#emergent-behavior) — Cross-lane emergent-behavior tagging: mark turn-sets for collusion/groupthink/cascade/role-drift
- [`episode_annotation`](#episode-annotation) — Embodied robot episode: synchronized video streams and time-series lanes with phase, outcome and dense-reward annotation
- [`error_span`](#error-span) — MQM-style error span annotation with typed severity for quality evaluation
- [`event_annotation`](#event-annotation) — N-ary event annotation with triggers and typed arguments
- [`extractive_qa`](#extractive-qa) — SQuAD-style extractive question answering with answer span highlighting
- [`failure_attribution`](#failure-attribution) — Multi-agent failure attribution: responsible agent + decisive step + reason
- [`grounding_eval`](#grounding-eval) — Grounding evaluation: bind referring expressions to image regions or points, with an explicit not-present answer
- [`gui_trajectory`](#gui-trajectory) — Computer-use/GUI agent step review: per-step screenshot + action correctness + click grounding
- [`handoff_review`](#handoff-review) — Annotate agent-to-agent handoffs: inter-agent misalignment flags + quality
- [`hierarchical_multiselect`](#hierarchical-multiselect) — Hierarchical tree-structured multi-label selection
- [`image_annotation`](#image-annotation) — Image annotation with bounding boxes, polygons, freeform drawing, and landmarks
- [`likert`](#likert) — Likert scale rating
- [`multi_document_event`](#multi-document-event) — Cross-document event annotation: template slots filled with evidence from many documents
- [`multimodal_reasoning`](#multimodal-reasoning) — Interleaved text/image/tool reasoning trace: per-step coherence + visual-hallucination rating
- [`multirate`](#multirate) — Rate multiple items on a scale
- [`multiselect`](#multiselect) — Multiple-choice checkbox selection
- [`number`](#number) — Numeric input field
- [`pairwise`](#pairwise) — Pairwise comparison of two items (binary selection or scale rating)
- [`process_reward`](#process-reward) — Per-step process reward signals for PRM training
- [`pure_display`](#pure-display) — Display-only content (instructions, headers)
- [`radio`](#radio) — Single-choice radio button selection
- [`range_slider`](#range-slider) — Dual-thumb slider for selecting an acceptable range
- [`ranking`](#ranking) — Drag-and-drop ranking of items by preference or relevance
- [`region_caption`](#region-caption) — Region captioning: a free-text description per region drawn on the image, with caption agreement over matched regions
- [`rollout_evaluation`](#rollout-evaluation) — World-model rollout evaluation: frame-locked video panels with temporal violation localization, preference and counterfactual plausibility
- [`rubric_eval`](#rubric-eval) — Multi-criteria rubric evaluation grid for LLM and text quality assessment
- [`select`](#select) — Dropdown selection
- [`semantic_differential`](#semantic-differential) — Bipolar adjective scales for measuring connotative meaning
- [`slider`](#slider) — Slider for selecting a value in a range
- [`soft_label`](#soft-label) — Probability distribution across labels via constrained sliders
- [`span`](#span) — Text span annotation/highlighting with optional entity linking to knowledge bases
- [`span_link`](#span-link) — Create relationships/links between spans (e.g., PERSON works_for ORGANIZATION)
- [`spatial_annotation`](#spatial-annotation) — 3D point cloud annotation with oriented cuboids, points, polylines, and per-point segments
- [`speech_transcript`](#speech-transcript) — Aligned-transcript speech-error annotation: per-segment ASR/TTS error tags + correction
- [`table_grid`](#table-grid) — Table-cell structure annotation: rows x cols grid + per-cell role (header/data/empty)
- [`temporal_grounding`](#temporal-grounding) — Video temporal grounding: mark gold event intervals with live IoU vs predicted
- [`text`](#text) — Free-form text input
- [`text_edit`](#text-edit) — Inline text editing with diff tracking for post-editing and correction tasks
- [`tiered_annotation`](#tiered-annotation) — Hierarchical multi-tier annotation for audio/video (ELAN-style)
- [`tool_call_review`](#tool-call-review) — Per-tool-call correctness review (right tool / args / ordering)
- [`tool_contention`](#tool-contention) — Tool/resource-contention timeline: per-agent lanes + shared-resource collision classification
- [`trajectory_edit`](#trajectory-edit) — Per-step trajectory correction/editing for SFT/DPO training data
- [`trajectory_eval`](#trajectory-eval) — Per-step trajectory evaluation with error taxonomy and severity scoring
- [`tree_annotation`](#tree-annotation) — Annotation of conversation tree nodes with path selection
- [`triage`](#triage) — Binary accept/reject triage for rapid data curation
- [`vas`](#vas) — Continuous visual analog scale for fine-grained magnitude estimation
- [`video`](#video) — Video player display
- [`video_annotation`](#video-annotation) — Video annotation with temporal segments, frame classification, keyframes, and object tracking
- [`voice_interaction`](#voice-interaction) — Voice/full-duplex turn-taking: dual-track timeline + barge-in/overlap classification

## agent_interaction_graph

Clickable agent-interaction graph: mark critical-path nodes + flag problematic edges

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_key`, `steps_key`

```yaml
- annotation_type: agent_interaction_graph
  name: graph
  description: Mark the critical path and flag any problematic handoffs.
  steps_key: steps
  agent_key: agent
```

From `examples/agent-traces/interaction-graph/config.yaml`.

## agent_scorecard

Per-agent + per-team scorecard with optional milestones (MultiAgentBench-style)

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_dimensions`, `agent_key`, `agents`, `milestones`, `scale`, `steps_key`, `team_dimensions`

```yaml
- annotation_type: agent_scorecard
  name: scorecard
  description: Score each agent, the team, and which milestones were reached.
  steps_key: steps
  agent_key: agent
  scale: 5
  agent_dimensions:
  - role fidelity
  - contribution
  - coordination
  team_dimensions:
  - coordination
  - communication
  - efficiency
  milestones:
  - plan produced
  - task delegated correctly
  - result verified
```

From `examples/agent-traces/agent-scorecard/config.yaml`.

## audio_annotation

Audio segmentation and annotation with waveform visualization

**Required:** nothing beyond `name` and `description`

**Optional:** `labels`, `max_segments`, `min_segments`, `mode`, `playback_rate_control`, `segment_schemes`, `source_field`, `spectrogram`, `spectrogram_options`, `waveform`, `zoom_enabled`

```yaml
- annotation_type: audio_annotation
  name: audio_segmentation
  description: Listen to the audio and create segments for different content types.
    Use [ and ] keys to set segment boundaries, then press Enter to create a segment.
  mode: label
  labels:
  - name: speech
    color: '#4ECDC4'
    key_value: '1'
  - name: music
    color: '#FF6B6B'
    key_value: '2'
  - name: silence
    color: '#95A5A6'
    key_value: '3'
  - name: noise
    color: '#F39C12'
    key_value: '4'
  min_segments: 1
  zoom_enabled: true
  playback_rate_control: true
```

From `examples/audio/audio-annotation/config.yaml`.

## bws

Best-Worst Scaling: select the best and worst item from a set

**Required:** nothing beyond `name` and `description`

**Optional:** `best_description`, `label_requirement`, `sequential_key_binding`, `tuple_size`, `worst_description`

```yaml
- annotation_type: bws
  name: sentiment_ibws
  description: Sentiment Intensity
  best_description: Which sentence expresses the MOST positive sentiment?
  worst_description: Which sentence expresses the LEAST positive sentiment?
  tuple_size: 4
  sequential_key_binding: true
```

From `examples/classification/iterative-bws/config.yaml`.

## card_sort

Drag-and-drop card sorting into predefined or user-created groups

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_empty_groups`, `allow_multiple`, `groups`, `items_field`, `mode`

```yaml
- annotation_type: card_sort
  name: topic_groups
  description: Drag each item into the topic group where it best fits
  mode: closed
  groups:
  - Science
  - Technology
  - Health
  - Business
  - Entertainment
  items_field: items
  allow_empty_groups: true
  allow_multiple: false
```

From `examples/classification/card-sort/config.yaml`.

## code_review

GitHub PR-style code review with inline comments and file ratings

**Required:** nothing beyond `name` and `description`

**Optional:** `comment_categories`, `file_rating_dimensions`, `verdict_options`

```yaml
- annotation_type: code_review
  name: review
  description: Review the agent's code changes
  comment_categories:
  - bug
  - style
  - suggestion
  - security
  - question
  verdict_options:
  - approve
  - request_changes
  - comment_only
  file_rating_dimensions:
  - correctness
  - quality
```

From `examples/agent-traces/coding-agent-review/config.yaml`.

## confidence

Confidence rating meta-annotation for any primary annotation

**Required:** nothing beyond `name` and `description`

**Optional:** `labels`, `left_label`, `max_value`, `min_value`, `right_label`, `scale_points`, `scale_type`, `step`, `target_schema`

```yaml
- annotation_type: confidence
  name: sentiment_confidence
  description: How confident are you in your label?
  target_schema: sentiment
  scale_type: likert
  scale_points: 5
  labels:
  - Guessing
  - Somewhat confident
  - Fairly confident
  - Confident
  - Certain
```

From `examples/classification/confidence-calibrated/config.yaml`.

## conjoint

Discrete choice conjoint analysis with side-by-side profile comparison

**Required:** nothing beyond `name` and `description`

**Optional:** `attributes`, `profiles_field`, `profiles_per_set`, `show_none_option`

```yaml
- annotation_type: conjoint
  name: assistant_preference
  description: Which AI assistant profile would you prefer to use? Compare the options
    and select your preferred one.
  profiles_per_set: 3
  attributes:
  - name: Response Speed
    levels:
    - Instant (under 1 second)
    - Fast (1-5 seconds)
    - Moderate (5-15 seconds)
  - name: Accuracy
    levels:
    - Mostly accurate with occasional errors
    - Highly accurate
    - Near-perfect accuracy
  - name: Conversation Style
    levels:
    - Brief and concise
    - Balanced
    - Detailed and thorough
  - name: Privacy Level
    levels:
    - Stores conversation history
    - Stores summaries only
    - No data retention
  show_none_option: true
```

From `examples/classification/conjoint/config.yaml`.

## consensus_tracking

Tag discussion acts per turn (proposal/agreement/disagreement/decision/concession) with cross-turn links to referenced proposals

**Required:** nothing beyond `name` and `description`

**Optional:** `acts`, `hint`, `linked_acts`, `turns_key`

```yaml
- annotation_type: consensus_tracking
  name: debate_moves
  description: Tag the debate structure
  turns_key: conversation
  acts:
  - claim
  - rebuttal
  - concession
  - ruling
  linked_acts:
  - rebuttal
  - concession
```

From `examples/agent-traces/debate-judging/config.yaml`.

## constant_sum

Allocate a fixed budget of points across categories

**Required:** `labels`

**Optional:** `input_type`, `min_per_item`, `total_points`

```yaml
- annotation_type: constant_sum
  name: topic_relevance
  description: Allocate 100 points across topics by relevance to the text (must sum
    to 100)
  labels:
  - Politics
  - Technology
  - Sports
  - Entertainment
  total_points: 100
  min_per_item: 0
  input_type: number
```

From `examples/classification/constant-sum/config.yaml`.

## context_attribution

Tag how each turn uses earlier context (used correctly / hallucinated / ignored) with links to the source turn

**Required:** nothing beyond `name` and `description`

**Optional:** `acts`, `hint`, `linked_acts`, `turns_key`

```yaml
- annotation_type: context_attribution
  name: memory_use
  description: How did the agent use its context?
  turns_key: conversation
```

From `examples/agent-traces/context-attribution/config.yaml`.

## coreference

Coreference chain annotation for grouping mentions of the same entity

**Required:** `span_schema`

**Optional:** `allow_singletons`, `entity_types`, `visual_display`

```yaml
- annotation_type: coreference
  name: coref_chains
  description: Group mentions that refer to the same entity into coreference chains
  span_schema: mentions
  entity_types:
  - PERSON
  - ORG
  - LOC
  - OTHER
  allow_singletons: true
  visual_display:
    highlight_mode: background
```

From `examples/span/coreference/config.yaml`.

## emergent_behavior

Cross-lane emergent-behavior tagging: mark turn-sets for collusion/groupthink/cascade/role-drift

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_key`, `allow_note`, `behaviors`, `steps_key`

```yaml
- annotation_type: emergent_behavior
  name: emergent
  description: For each collective behavior, check the turns (across agents) that
    participate.
  steps_key: steps
  agent_key: agent
  behaviors:
  - collusion
  - groupthink
  - cascading_error
  - role_drift
  allow_note: true
```

From `examples/agent-traces/emergent-behavior/config.yaml`.

## episode_annotation

Embodied robot episode: synchronized video streams and time-series lanes with phase, outcome and dense-reward annotation

**Required:** nothing beyond `name` and `description`

**Optional:** `episode_field`, `failure_causes`, `layers`, `max_lanes`, `max_phases`, `min_phases`, `outcomes`, `phases`, `reward_range`, `series_shown`, `source_field`

```yaml
- annotation_type: episode_annotation
  name: episode_review
  description: Mark the phases, judge the outcome, and draw the progress.
  source_field: episode
  layers:
  - phases
  - outcome
  - reward
  phases:
  - name: reach
    color: '#4ECDC4'
    key_value: '1'
  - name: grasp
    color: '#FFD93D'
    key_value: '2'
  - name: transport
    color: '#6C8AE4'
    key_value: '3'
  - name: place
    color: '#95E1A3'
    key_value: '4'
  - name: retract
    color: '#C9A0DC'
    key_value: '5'
  outcomes:
  - success
  - partial
  - failure
  failure_causes:
  - missed grasp
  - object slipped
  - collision
  - wrong object
  - wrong placement
  - timeout / stalled
  reward_range:
  - 0.0
  - 1.0
  series_shown:
  - gripper
  - wrist_force
  - shoulder_lift
  - elbow
```

From `examples/embodied/lerobot-episode/config.yaml`.

## error_span

MQM-style error span annotation with typed severity for quality evaluation

**Required:** `error_types`

**Optional:** `max_score`, `severities`, `show_score`

```yaml
- annotation_type: error_span
  name: mqm_errors
  description: Select error spans in the translation and classify each by type and
    severity
  error_types:
  - name: Accuracy
    subtypes:
    - Mistranslation
    - Omission
    - Addition
    - Untranslated
  - name: Fluency
    subtypes:
    - Grammar
    - Spelling
    - Punctuation
    - Register
  - name: Terminology
  - name: Style
  severities:
  - name: Minor
    weight: -1
  - name: Major
    weight: -5
  - name: Critical
    weight: -10
  show_score: true
  max_score: 100
```

From `examples/classification/error-span/config.yaml`.

## event_annotation

N-ary event annotation with triggers and typed arguments

**Required:** `event_types`, `span_schema`

**Optional:** `visual_display`

```yaml
- annotation_type: event_annotation
  name: events
  description: Annotate events with triggers and arguments
  span_schema: entities
  event_types:
  - type: ATTACK
    color: '#dc2626'
    trigger_labels:
    - EVENT_TRIGGER
    arguments:
    - role: attacker
      entity_types:
      - PERSON
      - ORGANIZATION
      required: true
    - role: target
      entity_types:
      - PERSON
      - ORGANIZATION
      - LOCATION
      required: true
    - role: weapon
      entity_types:
      - WEAPON
      required: false
    - role: location
      entity_types:
      - LOCATION
      required: false
  - type: HIRE
    color: '#2563eb'
    trigger_labels:
    - EVENT_TRIGGER
    arguments:
    - role: employer
      entity_types:
      - ORGANIZATION
      required: true
    - role: employee
      entity_types:
      - PERSON
      required: true
    - role: position
      required: false
  - type: TRAVEL
    color: '#16a34a'
    trigger_labels:
    - EVENT_TRIGGER
    arguments:
    - role: traveler
      entity_types:
      - PERSON
      required: true
    - role: origin
      entity_types:
      - LOCATION
      required: false
    - role: destination
      entity_types:
      - LOCATION
      required: true
  visual_display:
    enabled: true
    arc_position: above
    show_labels: true
```

From `examples/span/event-annotation/config.yaml`.

## extractive_qa

SQuAD-style extractive question answering with answer span highlighting

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_unanswerable`, `highlight_color`, `passage_field`, `question_field`

```yaml
- annotation_type: extractive_qa
  name: answer_span
  description: Highlight the span in the passage that answers the question
  question_field: question
  passage_field: passage
  allow_unanswerable: true
  highlight_color: '#FFEB3B'
```

From `examples/classification/extractive-qa/config.yaml`.

## failure_attribution

Multi-agent failure attribution: responsible agent + decisive step + reason

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_key`, `agents`, `steps_key`

```yaml
- annotation_type: failure_attribution
  name: attribution
  description: 'If it failed: which agent, which step, and why?'
  steps_key: steps
  agent_key: agent
```

From `examples/agent-traces/failure-attribution/config.yaml`.

## grounding_eval

Grounding evaluation: bind referring expressions to image regions or points, with an explicit not-present answer

**Required:** nothing beyond `name` and `description`

**Optional:** `caption_field`, `expression_source`, `expressions_field`, `label`, `predictions_field`, `region_type`, `require_all`, `verdicts`

```yaml
- annotation_type: grounding_eval
  name: grounding
  description: What does each phrase refer to?
  region_type: box
  expressions_field: expressions
```

From `examples/ai-assisted/grounding-eval/config.yaml`.

## gui_trajectory

Computer-use/GUI agent step review: per-step screenshot + action correctness + click grounding

**Required:** nothing beyond `name` and `description`

**Optional:** `action_key`, `coord_space`, `screenshot_key`, `steps_key`, `verdict_options`

```yaml
- annotation_type: gui_trajectory
  name: gui_review
  description: 'For each step: was the action correct and did the click land on the
    right element?'
  steps_key: steps
  screenshot_key: screenshot
  action_key: action
  coord_space: normalized
  verdict_options:
  - correct
  - wrong_element
  - wrong_action
  - hallucinated
```

From `examples/agent-traces/gui-trajectory/config.yaml`.

## handoff_review

Annotate agent-to-agent handoffs: inter-agent misalignment flags + quality

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_key`, `flags`, `quality_scale`, `steps_key`

```yaml
- annotation_type: handoff_review
  name: handoffs
  description: 'For each handoff: flag any misalignment and rate the quality.'
  steps_key: steps
  agent_key: agent
  flags:
  - info_loss
  - dropped_constraint
  - garbling
  - goal_drift
  quality_scale: 5
```

From `examples/agent-traces/handoff-review/config.yaml`.

## hierarchical_multiselect

Hierarchical tree-structured multi-label selection

**Required:** `taxonomy`

**Optional:** `auto_select_children`, `auto_select_parent`, `max_selections`, `show_search`, `taxonomy_preset`, `tooltips`

```yaml
- annotation_type: hierarchical_multiselect
  name: topic_hierarchy
  description: Select all topics that apply to this text. You may select at any level
    of the hierarchy.
  taxonomy:
    Science:
      Physics:
      - Quantum Mechanics
      - Thermodynamics
      Biology:
      - Genetics
      - Ecology
    Technology:
    - Artificial Intelligence
    - Blockchain
    - Cybersecurity
    Arts:
    - Music
    - Painting
    - Literature
  auto_select_children: false
  auto_select_parent: false
  show_search: true
```

From `examples/classification/hierarchical-multiselect/config.yaml`.

## image_annotation

Image annotation with bounding boxes, polygons, freeform drawing, and landmarks

**Required:** `labels`, `tools`

**Optional:** `ai_support`, `brush_size`, `carry_over`, `eraser_size`, `fill_max_pixels`, `fill_mode`, `fill_tolerance`, `freeform_brush_size`, `freeform_simplify`, `instance_masks`, `keybinding_profile`, `mask_mode`, `mask_opacity`, `max_annotations`, `min_annotations`, `pan_enabled`, `segmentation`, `skeletons`, `source_field`, `text_prompt`, `tiles`, `viewer`, `zoom_enabled`

```yaml
- annotation_type: image_annotation
  name: object_detection
  description: Draw boxes around objects you see in the image. Use the polygon tool
    for irregular shapes.
  tools:
  - bbox
  - polygon
  labels:
  - name: person
    color: '#FF6B6B'
    key_value: '1'
  - name: vehicle
    color: '#4ECDC4'
    key_value: '2'
  - name: animal
    color: '#45B7D1'
    key_value: '3'
  - name: object
    color: '#96CEB4'
    key_value: '4'
  min_annotations: 1
  zoom_enabled: true
  pan_enabled: true
```

From `examples/image/image-annotation/config.yaml`.

## likert

Likert scale rating

**Required:** `max_label`, `min_label`, `size`

**Optional:** `bad_text_label`, `displaying_score`, `label_requirement`, `labels`, `sequential_key_binding`

```yaml
- annotation_type: likert
  name: awesomeness
  description: How awesome is this?
  min_label: Not Awesome
  max_label: Completely Awesome
  size: 5
  sequential_key_binding: true
```

From `examples/classification/likert/config.yaml`.

## multi_document_event

Cross-document event annotation: template slots filled with evidence from many documents

**Required:** `slots`

**Optional:** `allow_annotator_create`, `template_name`

```yaml
- annotation_type: multi_document_event
  name: events
  description: Group documents into disaster events and fill each slot with evidence
  slots:
  - name: event_type
    description: Kind of disaster
    type: text
  - name: who_affected
    description: People/communities affected
    type: text
  - name: what_damaged
    description: Property, infrastructure, or economy damaged
    type: text
  - name: where
    description: Location of the event
    type: text
  - name: when
    description: When the event occurred
    type: text
  allow_annotator_create: true
```

From `examples/advanced/multi-document-events/config.yaml`.

## multimodal_reasoning

Interleaved text/image/tool reasoning trace: per-step coherence + visual-hallucination rating

**Required:** nothing beyond `name` and `description`

**Optional:** `steps_key`, `type_key`, `verdict_options`

```yaml
- annotation_type: multimodal_reasoning
  name: reasoning_review
  description: 'Judge each step: is the reasoning coherent and the visual grounded
    (not hallucinated)?'
  steps_key: steps
  type_key: type
  verdict_options:
  - coherent
  - incoherent
  - visual_hallucination
  - uncertain
```

From `examples/agent-traces/multimodal-reasoning/config.yaml`.

## multirate

Rate multiple items on a scale

**Required:** `labels`, `options`

**Optional:** `arrangement`, `display_config`, `label_requirement`, `options_from_data`, `randomize_order`

```yaml
- annotation_type: multirate
  name: awesomeness
  description: How awesome is this?
  display_config:
    num_columns: 1
  options:
  - Option 1
  - Option 2
  - Option 3
  labels:
  - Strongly disagree
  - Disagree
  - Neutral
  - Agree
  - Strongly Agree
  label_requirement:
    required: true
  sequential_key_binding: true
  option_randomization: true
```

From `examples/classification/multirate/config.yaml`.

## multiselect

Multiple-choice checkbox selection

**Required:** `labels`

**Optional:** `codebook`, `display_config`, `dynamic_options`, `dynamic_options_field`, `has_free_response`, `label_requirement`, `option_randomization`, `randomize_order`, `sequential_key_binding`, `video_as_label`

```yaml
- annotation_type: multiselect
  name: favorite_color
  description: What colors are mentioned in the text?
  labels:
  - blue
  - maize
  - green
  - white
  sequential_key_binding: true
```

From `examples/classification/check-box/config.yaml`.

## number

Numeric input field

**Required:** nothing beyond `name` and `description`

**Optional:** `custom_css`, `label_requirement`, `max`, `max_value`, `min`, `min_value`, `step`, `tooltip`, `tooltip_file`

```yaml
- annotation_type: number
  name: numeric_input
  description: '**Number Input** - Enter a numeric value (e.g., word count estimate)'
  min: 0
  max: 1000
  step: 1
```

From `examples/advanced/all-annotation-types/config.yaml`.

## pairwise

Pairwise comparison of two items (binary selection or scale rating)

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_tie`, `dimensions`, `items`, `items_key`, `justification`, `label_requirement`, `labels`, `mode`, `randomize_order`, `scale`, `sequential_key_binding`, `show_labels`, `tie_label`

```yaml
- annotation_type: pairwise
  name: preference_scale
  description: Rate how much more intimate one tweet is compared to the other
  mode: scale
  items_key: text
  show_labels: true
  labels:
  - Tweet A
  - Tweet B
  scale:
    min: -3
    max: 3
    step: 1
    default: 0
    labels:
      min: A is much more intimate
      max: B is much more intimate
      center: Equal / Hard to tell
  label_requirement:
    required: true
```

From `examples/classification/pairwise-scale/config.yaml`.

## process_reward

Per-step process reward signals for PRM training

**Required:** nothing beyond `name` and `description`

**Optional:** `ai_prelabel`, `allow_neutral`, `inline_with_trace`, `mode`, `require_verification`, `reward_labels`, `step_text_key`, `steps_key`

```yaml
- annotation_type: process_reward
  name: step_rewards
  description: 'Verify the reward for each reasoning step: ✓ correct, ○ neutral, ✗
    incorrect'
  steps_key: cot_steps
  step_text_key: text
  mode: per_step
  allow_neutral: true
  inline_with_trace: true
  ai_prelabel: true
  require_verification: true
```

From `examples/agent-traces/cot-process-reward/config.yaml`.

## pure_display

Display-only content (instructions, headers)

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_html`, `labels`

```yaml
- annotation_type: pure_display
  name: selection_header
  description: '## Selection Types'
```

From `examples/advanced/all-annotation-types/config.yaml`.

## radio

Single-choice radio button selection

**Required:** `labels`

**Optional:** `codebook`, `dynamic_options`, `dynamic_options_field`, `has_free_response`, `horizontal`, `label_requirement`, `option_randomization`, `randomize_order`, `sequential_key_binding`

```yaml
- annotation_type: radio
  name: favorite_color
  description: What food does this text make you want to eat?
  labels:
  - pizza
  - bagels
  - burgers
  - curry
  - tacos
  sequential_key_binding: true
```

From `examples/classification/single-choice/config.yaml`.

## range_slider

Dual-thumb slider for selecting an acceptable range

**Required:** nothing beyond `name` and `description`

**Optional:** `left_label`, `max_value`, `min_value`, `right_label`, `show_values`, `step`

```yaml
- annotation_type: range_slider
  name: formality_range
  description: What range of formality is appropriate for this text? Drag both handles
    to indicate the acceptable range.
  min_value: 0
  max_value: 100
  step: 1
  left_label: Very informal
  right_label: Very formal
  show_values: true
```

From `examples/classification/range-slider/config.yaml`.

## ranking

Drag-and-drop ranking of items by preference or relevance

**Required:** `labels`

**Optional:** `allow_ties`

```yaml
- annotation_type: ranking
  name: response_quality
  description: Rank the responses from best (1st) to worst (last) based on helpfulness
    and accuracy
  labels:
  - Response A
  - Response B
  - Response C
  - Response D
  allow_ties: false
```

From `examples/classification/ranking/config.yaml`.

## region_caption

Region captioning: a free-text description per region drawn on the image, with caption agreement over matched regions

**Required:** nothing beyond `name` and `description`

**Optional:** `agreement_distance`, `max_length`, `min_length`, `placeholder`, `require_all`

```yaml
- annotation_type: region_caption
  name: captions
  description: Describe each region you drew
  placeholder: e.g. a red mug with a chipped handle
  min_length: 10
  max_length: 300
  require_all: true
  agreement_distance: token
```

From `examples/image/region-captioning/config.yaml`.

## rollout_evaluation

World-model rollout evaluation: frame-locked video panels with temporal violation localization, preference and counterfactual plausibility

**Required:** nothing beyond `name` and `description`

**Optional:** `blind`, `cf_verdicts`, `fps`, `intervention_field`, `intervention_time_field`, `layers`, `manifest_field`, `max_violations`, `prompt_field`, `require_clean`, `rubric`, `severities`, `shuffle`, `streams`, `violation_types`

```yaml
- annotation_type: rollout_evaluation
  name: rollout_review
  description: Where does each rollout stop making sense?
  streams:
  - field: real
    name: Recording
    role: real
  - field: gen_a
    name: Model A
  - field: gen_b
    name: Model B
  fps: 25
  prompt_field: prompt
  intervention_field: intervention
  intervention_time_field: intervention_t
  layers:
  - violations
  - preference
  - counterfactual
  blind: true
  shuffle: true
  require_clean: true
  violation_types:
  - name: object_permanence
    description: An object vanishes, or appears, with nothing causing it.
  - name: rigid_body_violation
    description: A solid object bends, stretches or changes size.
  - name: interpenetration
    description: Two solid objects pass through each other.
  - name: gravity_violation
    description: Something floats, falls upward, or stands unsupported.
  - name: causality_violation
    description: An effect happens before, or without, its cause.
  - name: identity_flicker
    description: An object swaps identity or category between frames.
  - name: appearance_drift
    description: Texture, colour or shape drifts with no event to explain it.
  severities:
  - value: 1
    name: subtle
    description: Visible on a second look.
  - value: 2
    name: clear
    description: Obvious on first viewing.
  - value: 3
    name: breaks the scene
    description: Nothing after this point is worth judging.
  rubric:
    physics: Do objects move and interact the way real objects would?
    fidelity: Does it look like the same scene throughout?
    instruction: Does it do what the scenario describes?
```

From `examples/agent-traces/world-model-rollouts/config.yaml`.

## rubric_eval

Multi-criteria rubric evaluation grid for LLM and text quality assessment

**Required:** `criteria`

**Optional:** `scale_labels`, `scale_points`, `show_overall`

```yaml
- annotation_type: rubric_eval
  name: response_quality
  description: Evaluate the AI response on each criterion
  scale_points: 5
  scale_labels:
  - Poor
  - Below Average
  - Average
  - Good
  - Excellent
  criteria:
  - name: accuracy
    description: Are the facts and claims in the response correct?
  - name: completeness
    description: Does the response address all aspects of the question?
  - name: clarity
    description: Is the response well-organized and easy to understand?
  - name: helpfulness
    description: Would this response be useful to someone asking the question?
  show_overall: true
```

From `examples/classification/rubric-eval/config.yaml`.

## select

Dropdown selection

**Required:** `labels`

**Optional:** `codebook`, `dynamic_options`, `dynamic_options_field`, `label_requirement`, `option_randomization`, `randomize_order`, `use_predefined_labels`

```yaml
- annotation_type: select
  name: dropdown_choice
  description: '**Dropdown** - Select from a list'
  labels:
  - news
  - opinion
  - review
  - social_media
  - academic
  - other
```

From `examples/advanced/all-annotation-types/config.yaml`.

## semantic_differential

Bipolar adjective scales for measuring connotative meaning

**Required:** `pairs`

**Optional:** `scale_points`

```yaml
- annotation_type: semantic_differential
  name: word_connotation
  description: Rate the word or concept on each of these dimensions (1 = left pole,
    7 = right pole)
  scale_points: 7
  pairs:
  - - Good
    - Bad
  - - Strong
    - Weak
  - - Active
    - Passive
  - - Happy
    - Sad
```

From `examples/classification/semantic-differential/config.yaml`.

## slider

Slider for selecting a value in a range

**Required:** `max_value`, `min_value`, `starting_value`

**Optional:** `label_requirement`, `labels`, `maxTick`, `show_labels`, `step`

```yaml
- annotation_type: slider
  name: awesomeness
  description: How awesome is this?
  min_value: 0
  max_value: 100
  starting_value: 50
```

From `examples/classification/slider/config.yaml`.

## soft_label

Probability distribution across labels via constrained sliders

**Required:** `labels`

**Optional:** `min_per_label`, `show_distribution_chart`, `total`

```yaml
- annotation_type: soft_label
  name: sentiment_dist
  description: Distribute probability across sentiment labels (must sum to 100)
  labels:
  - Positive
  - Neutral
  - Negative
  total: 100
  min_per_label: 0
  show_distribution_chart: true
```

From `examples/classification/soft-label/config.yaml`.

## span

Text span annotation/highlighting with optional entity linking to knowledge bases

**Required:** `labels`

**Optional:** `allow_discontinuous`, `bad_text_label`, `codebook`, `columns`, `displaying_score`, `entity_linking`, `sequential_key_binding`, `show_span_labels`, `target_field`, `title`

```yaml
- annotation_type: span
  name: certainty
  description: Highlight which phrases make the sentence more or less certain
  labels:
  - certain
  - uncertain
  sequential_key_binding: true
```

From `examples/span/span-labeling/config.yaml`.

## span_link

Create relationships/links between spans (e.g., PERSON works_for ORGANIZATION)

**Required:** `link_types`, `span_schema`

**Optional:** `visual_display`

```yaml
- annotation_type: span_link
  name: relations
  description: Annotate relationships between entities
  span_schema: entities
  link_types:
  - name: WORKS_FOR
    directed: true
    allowed_source_labels:
    - PERSON
    allowed_target_labels:
    - ORGANIZATION
    color: '#dc2626'
  - name: LOCATED_IN
    directed: true
    allowed_source_labels:
    - PERSON
    - ORGANIZATION
    allowed_target_labels:
    - LOCATION
    color: '#8b5cf6'
  - name: COLLABORATES_WITH
    directed: false
    allowed_source_labels:
    - PERSON
    allowed_target_labels:
    - PERSON
    color: '#06b6d4'
  - name: SUPERVISES
    directed: true
    allowed_source_labels:
    - PERSON
    allowed_target_labels:
    - PERSON
    color: '#ec4899'
  - name: FOUNDED
    directed: true
    allowed_source_labels:
    - PERSON
    allowed_target_labels:
    - ORGANIZATION
    color: '#14b8a6'
  - name: ACQUIRED
    directed: true
    allowed_source_labels:
    - ORGANIZATION
    allowed_target_labels:
    - ORGANIZATION
    color: '#f97316'
  visual_display:
    enabled: true
    arc_position: above
    show_labels: true
```

From `examples/span/span-linking/config.yaml`.

## spatial_annotation

3D point cloud annotation with oriented cuboids, points, polylines, and per-point segments

**Required:** `labels`, `tools`

**Optional:** `calibration_field`, `color_mode`, `default_box_height`, `fit_box_height`, `lod`, `max_annotations`, `max_loaded_nodes`, `max_points`, `min_annotations`, `min_screen_size`, `mpr`, `point_budget`, `point_size`, `slab_thickness`, `source_field`

```yaml
- annotation_type: spatial_annotation
  name: objects
  description: Put a 3D box around every vehicle and pedestrian.
  source_field: point_cloud
  tools:
  - cuboid_3d
  - point_3d
  labels:
  - name: car
    color: '#FF6B6B'
    key_value: '1'
  - name: truck
    color: '#4ECDC4'
    key_value: '2'
  - name: pedestrian
    color: '#FFD93D'
    key_value: '3'
  color_mode: height
  point_size: 2.0
  lod: true
  max_points: 400000
  slab_thickness: 2.0
  calibration_field: calibration
  default_box_height: 1.7
  fit_box_height: true
```

From `examples/spatial/kitti-cuboids/config.yaml`.

## speech_transcript

Aligned-transcript speech-error annotation: per-segment ASR/TTS error tags + correction

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_correction`, `audio_key`, `error_types`, `segments_key`, `speaker_key`, `text_key`, `turns_key`

```yaml
- annotation_type: speech_transcript
  name: speech_errors
  description: Tag speech errors on each segment and correct the transcript where
    needed.
  segments_key: segments
  error_types:
  - asr_error
  - tts_artifact
  - mispronunciation
  - disfluency
  allow_correction: true
```

From `examples/agent-traces/speech-transcript/config.yaml`.

## table_grid

Table-cell structure annotation: rows x cols grid + per-cell role (header/data/empty)

**Required:** nothing beyond `name` and `description`

**Optional:** `cols_key`, `default_cols`, `default_rows`, `image_key`, `roles`, `rows_key`

```yaml
- annotation_type: table_grid
  name: structure
  description: Set the grid size, then click cells to mark column/row headers and
    empty cells.
  image_key: image
  rows_key: rows
  cols_key: cols
  roles:
  - data
  - col_header
  - row_header
  - empty
```

From `examples/agent-traces/table-grid/config.yaml`.

## temporal_grounding

Video temporal grounding: mark gold event intervals with live IoU vs predicted

**Required:** nothing beyond `name` and `description`

**Optional:** `duration`, `events_key`, `video_key`

```yaml
- annotation_type: temporal_grounding
  name: grounding
  description: For each event, mark the gold start/end interval. IoU vs prediction
    updates live.
  video_key: video
  events_key: events
```

From `examples/agent-traces/temporal-grounding/config.yaml`.

## text

Free-form text input

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_paste`, `collapsible`, `cols`, `display_config`, `label_requirement`, `labels`, `min_chars`, `multiline`, `placeholder`, `rows`, `show_char_count`, `target_schema`, `textarea`

```yaml
- annotation_type: text
  name: textbox_input
  description: How does this text make you feel?
  labels:
  - happy?
  - sad?
  textarea:
    'on': false
    rows: 2
    cols: 40
```

From `examples/classification/text-box/config.yaml`.

## text_edit

Inline text editing with diff tracking for post-editing and correction tasks

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_reset`, `show_diff`, `show_edit_distance`, `source_field`

```yaml
- annotation_type: text_edit
  name: post_edit
  description: Edit the machine translation output to produce a fluent, accurate translation
  source_field: mt_output
  show_diff: true
  show_edit_distance: true
  allow_reset: true
```

From `examples/classification/text-edit/config.yaml`.

## tiered_annotation

Hierarchical multi-tier annotation for audio/video (ELAN-style)

**Required:** `source_field`, `tiers`

**Optional:** `audio_key`, `collapsed_tiers`, `media_type`, `overview_height`, `playback_rate_control`, `show_tier_labels`, `speaker_key`, `text_key`, `tier_height`, `transcript_field`, `transcript_tier`, `turns_key`, `zoom_enabled`

```yaml
- annotation_type: tiered_annotation
  name: linguistic_tiers
  description: Annotate speech with hierarchical tiers (utterance > word > phoneme)
  source_field: audio_url
  media_type: audio
  tiers:
  - name: utterance
    tier_type: independent
    description: Speaker utterances
    labels:
    - name: Speaker_A
      color: '#4ECDC4'
      tooltip: Utterance by Speaker A
    - name: Speaker_B
      color: '#FF6B6B'
      tooltip: Utterance by Speaker B
    - name: Overlap
      color: '#F39C12'
      tooltip: Overlapping speech
  - name: word
    tier_type: dependent
    parent_tier: utterance
    constraint_type: time_subdivision
    description: Word-level transcription
    labels:
    - name: Content
      color: '#95E1D3'
      tooltip: Content word
    - name: Function
      color: '#AA96DA'
      tooltip: Function word (articles, prepositions, etc.)
  - name: phoneme
    tier_type: dependent
    parent_tier: word
    constraint_type: time_subdivision
    description: Phoneme segments
    labels:
    - name: Vowel
      color: '#F38181'
    - name: Consonant
      color: '#95E1D3'
  - name: gesture
    tier_type: independent
    description: Non-verbal gestures and events
    labels:
    - name: Point
      color: '#DDA0DD'
    - name: Nod
      color: '#87CEEB'
    - name: Shake
      color: '#98D8C8'
    - name: Pause
      color: '#C0C0C0'
  tier_height: 50
  show_tier_labels: true
  zoom_enabled: true
  playback_rate_control: true
```

From `examples/audio/tiered-annotation/config.yaml`.

## tool_call_review

Per-tool-call correctness review (right tool / args / ordering)

**Required:** nothing beyond `name` and `description`

**Optional:** `steps_key`, `verdict_options`

```yaml
- annotation_type: tool_call_review
  name: tool_review
  description: 'Judge each tool call: right tool? correct arguments?'
  steps_key: steps
```

From `examples/agent-traces/tool-call-review/config.yaml`.

## tool_contention

Tool/resource-contention timeline: per-agent lanes + shared-resource collision classification

**Required:** nothing beyond `name` and `description`

**Optional:** `agent_key`, `calls_key`, `contention_labels`, `resource_key`

```yaml
- annotation_type: tool_contention
  name: contention
  description: Classify each shared-resource contention region.
  calls_key: calls
  agent_key: agent
  resource_key: resource
  contention_labels:
  - deadlock
  - circular_wait
  - race_condition
  - benign
```

From `examples/agent-traces/tool-contention/config.yaml`.

## trajectory_edit

Per-step trajectory correction/editing for SFT/DPO training data

**Required:** nothing beyond `name` and `description`

**Optional:** `allow_reset`, `edit_final_answer`, `editable_fields`, `final_answer_key`, `require_reason_on_edit`, `show_diff`, `show_edit_distance`, `step_text_key`, `steps_key`

```yaml
- annotation_type: trajectory_edit
  name: corrected_trajectory
  description: Fix any wrong steps, then correct the final answer
  steps_key: steps
  step_text_key: action
  editable_fields:
  - action
  show_diff: true
  show_edit_distance: true
  allow_reset: true
  edit_final_answer: true
  final_answer_key: final_answer
```

From `examples/agent-traces/trajectory-correction/config.yaml`.

## trajectory_eval

Per-step trajectory evaluation with error taxonomy and severity scoring

**Required:** nothing beyond `name` and `description`

**Optional:** `correctness_options`, `error_types`, `max_score`, `severities`, `show_score`, `step_text_key`, `steps_key`

```yaml
- annotation_type: trajectory_eval
  name: step_evaluation
  description: Evaluate each agent step for correctness and errors
  steps_key: steps
  step_text_key: action
  correctness_options:
  - correct
  - incorrect
  - partially_correct
  error_types:
  - name: reasoning
    subtypes:
    - logical_error
    - factual_error
    - planning_error
  - name: execution
    subtypes:
    - wrong_tool
    - wrong_args
    - api_error
  - name: safety
    subtypes:
    - harmful_action
    - data_leak
    - scope_violation
  severities:
  - name: minor
    weight: -1
  - name: major
    weight: -5
  - name: critical
    weight: -10
  show_score: true
  max_score: 100
```

From `examples/agent-traces/trajectory-evaluation/config.yaml`.

## tree_annotation

Annotation of conversation tree nodes with path selection

**Required:** nothing beyond `name` and `description`

**Optional:** `branch_comparison`, `node_scheme`, `path_selection`

```yaml
- annotation_type: tree_annotation
  name: response_quality
  description: Rate each response and select the best path through the conversation
  node_scheme:
    annotation_type: likert
    size: 5
    min_label: Poor
    max_label: Excellent
  path_selection:
    enabled: true
    description: Select the best response path by clicking nodes in order
```

From `examples/span/conversation-tree/config.yaml`.

## triage

Binary accept/reject triage for rapid data curation

**Required:** nothing beyond `name` and `description`

**Optional:** `accept_key`, `accept_label`, `auto_advance`, `reject_key`, `reject_label`, `show_progress`, `skip_key`, `skip_label`

```yaml
- annotation_type: triage
  name: data_quality
  description: Is this data sample suitable for training?
  auto_advance: true
  show_progress: true
```

From `examples/advanced/triage/config.yaml`.

## vas

Continuous visual analog scale for fine-grained magnitude estimation

**Required:** nothing beyond `name` and `description`

**Optional:** `left_label`, `max_value`, `min_value`, `precision`, `right_label`, `show_value`

```yaml
- annotation_type: vas
  name: pain_level
  description: Rate the pain level described in this text on a continuous scale
  left_label: No pain
  right_label: Worst pain imaginable
  min_value: 0
  max_value: 100
  show_value: true
```

From `examples/classification/vas/config.yaml`.

## video

Video player display

**Required:** `video_path`

**Optional:** `additional_sources`, `autoplay`, `controls`, `custom_css`, `fallback_text`, `loop`, `muted`

```yaml
- annotation_type: video
  name: stimulus
  description: Watch this before answering.
  video_path: https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4
  controls: true
  muted: true
```

From `examples/video/video-player/config.yaml`.

## video_annotation

Video annotation with temporal segments, frame classification, keyframes, and object tracking

**Required:** `labels`

**Optional:** `ai_support`, `frame_stepping`, `max_segments`, `min_segments`, `mode`, `overview_height`, `playback_rate_control`, `segment_schemes`, `show_timecode`, `source_field`, `timeline_height`, `tracking_options`, `video_fps`, `zoom_enabled`

```yaml
- annotation_type: video_annotation
  name: video_segments
  description: Watch the video and mark segments for different content types. Press
    [ to mark segment start, ] to mark segment end, then Enter to create the segment.
  mode: segment
  labels:
  - name: intro
    color: '#4ECDC4'
    key_value: '1'
  - name: main_content
    color: '#FF6B6B'
    key_value: '2'
  - name: transition
    color: '#45B7D1'
    key_value: '3'
  - name: outro
    color: '#95A5A6'
    key_value: '4'
  min_segments: 1
  timeline_height: 70
  zoom_enabled: true
  playback_rate_control: true
  frame_stepping: true
  show_timecode: true
  video_fps: 30
```

From `examples/video/video-frame-annotation/config.yaml`.

## voice_interaction

Voice/full-duplex turn-taking: dual-track timeline + barge-in/overlap classification

**Required:** nothing beyond `name` and `description`

**Optional:** `audio_key`, `overlap_labels`, `rating_scale`, `speaker_key`, `text_key`, `turns_key`, `user_speakers`

```yaml
- annotation_type: voice_interaction
  name: turn_taking
  description: Classify each barge-in/overlap and rate the overall turn-taking.
  turns_key: turns
  speaker_key: speaker
  user_speakers:
  - user
  - human
  - caller
  overlap_labels:
  - agent_should_respond
  - agent_should_resume
  - backchannel
  - uncertain
  rating_scale: 5
```

From `examples/agent-traces/voice-interaction/config.yaml`.
